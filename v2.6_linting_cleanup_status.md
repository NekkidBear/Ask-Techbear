# v2.6 Cleanup Status

## Flake8

Target: zero findings.

## Pyright

Remaining findings are known SQLAlchemy ORM typing artifacts.

Deferred until SQLAlchemy 2.0 typed-model migration:

- Migrate `Column(...)` declarations to `Mapped[]` and `mapped_column()`.
- Resolve ORM field assignment type warnings.
- Resolve `Column[UUID]` truthiness warnings.
- Resolve ORM field argument type warnings.

## Pylint

Remaining findings are primarily:

- Complexity warnings
- Duplicate-code warnings
- Import-resolution warnings

Address opportunistically during future refactors.
