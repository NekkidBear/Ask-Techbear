# RAG System TODOs (TechBear)

This document tracks non-blocking improvements, cleanup tasks, and future enhancements for the TechBear RAG + benchmarking system.

These items are intentionally deferred to avoid slowing down current experimental development.

---

## 🧹 Linting & Code Style (LOW PRIORITY)

These are safe to ignore during active experimentation but should be cleaned up later for maintainability.

### Import Ordering (PEP8 / pylint)
- Ensure standard library imports come first
- Then third-party imports (e.g., chromadb, requests)
- Then local application imports

Status: Deferred (non-functional)

---

### Missing Docstrings
- Add docstrings to service-layer modules:
  - backend/services/rag/retriever.py
  - backend/services/rag/rag.py (if used)
- Benchmarking scripts may remain lightly documented

Status: Deferred for scripts, recommended for services

---

### Final Newline / Formatting
- Ensure all Python files end with a newline
- Run formatter (optional future step: black or ruff)

Status: Cosmetic only

---

## ⚠️ IMPORTANT: DO NOT IGNORE

These are NOT style issues and must remain correct:

- Retrieval correctness (facts vs voice separation)
- Deterministic dataset ordering in benchmarking
- Mode isolation (raw / prompt_only / rag_facts / rag_full)
- Consistent return schema from pipeline functions
- Import path correctness after refactor into backend.services.rag

---

## 🔧 Future Improvements (MEDIUM PRIORITY)

### Retriever Enhancements
- Add hybrid retrieval scoring (facts + voice weighting)
- Add metadata filtering (series, date, voice_score)
- Add reranking layer (MMR or embedding similarity boost)

---

### Benchmarking Improvements
- Add JSONL output in addition to CSV
- Add reproducible run seeds (where applicable)
- Add experiment metadata tracking:
  - model
  - mode
  - timestamp
  - corpus version

---

### Pipeline Improvements
- Separate system prompt versions per mode (raw vs RAG)
- Add token usage tracking (if available from Ollama)
- Add structured prompt logging for debugging

---

## 🚀 Future Architecture (LONG TERM)

- Move retriever into pluggable interface:
  - ChromaRetriever
  - Future: pgvector retriever
- Add evaluation layer:
  - LLM-as-judge scoring
  - factuality scoring
  - voice adherence scoring
- Add API layer integration:
  - reuse RAG pipeline for production chat endpoint

---

## 🧠 Guiding Principle

Do not optimize style while the experimental system is still evolving.

Prioritize:
1. Correctness
2. Reproducibility
3. Isolation of experimental variables
4. Then code cleanliness